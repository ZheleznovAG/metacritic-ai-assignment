# `SPK-01` — доступ к Metacritic и ограничения источника

## Вопрос, граница и решение

**Вопрос:** существует ли технически воспроизводимый и допустимый способ регулярно получать данные с двух страниц Metacritic, указанных в задании?

**Проверено:** 2026-09-04, 18:20 `Asia/Novosibirsk` (`11:20 UTC`).

**Среда:** локальная рабочая машина; прямой HTTPS GET без аутентификации, cookies, browser session и обхода защит. Целевая hosting-среда ещё не выбрана, поэтому доступ из неё не проверялся.

**Итоговое решение:** `Ask`.

Обе страницы технически доступны обычному идентифицируемому HTTP-клиенту и возвращают исходный HTML с заголовками разделов и ссылками на игры. Однако официальный Terms of Use, на который ведёт footer Metacritic, запрещает автоматизированный доступ и scraping без предварительного письменного разрешения Fandom. Наличие такого разрешения или одобренного интерфейса в материалах проекта не подтверждено. `robots.txt` не запрещает нужные пути для общего user-agent, но не заменяет разрешение из Terms.

До ответа нельзя начинать `SPK-02` на живом контенте или проектировать production-сборщик как будто доступ разрешён. Это исследовательский вывод, а не юридическое заключение.

## Минимальный эксперимент

Для каждой URL выполнено два последовательных низкочастотных запроса с одним user-agent:

```powershell
curl.exe --location --compressed --max-time 30 `
  --user-agent "metacritic-ai-assignment-feasibility/0.1 (+https://github.com/ZheleznovAG/metacritic-ai-assignment)" `
  --output <temporary-file> `
  <url>
```

Тела ответов использовались только во временном каталоге для вычисления SHA-256 и проверки структурных маркеров, затем удалены. Они не добавлены в Git, чтобы не публиковать изменчивый сторонний контент до выяснения разрешения. Ни секреты, ни персональные данные в запросы не передавались.

### HTTP-результаты

`size_download` ниже — размер сжатого тела в транспортном ответе; SHA-256 вычислен для распакованного тела. Время взято из заголовка `Date` сервера.

| URL | Попытка | UTC | HTTP / тип | `size_download` | SHA-256 распакованного тела |
|---|---:|---|---|---:|---|
| <https://www.metacritic.com/game/> | 1 | 11:20:31 | `200`, `text/html;charset=utf-8` | 138901 | `0DBEBBDF297BA01CC49427E158519D1BDB1DFA0E15C154698B58C671D69409BA` |
| <https://www.metacritic.com/game/> | 2 | 11:20:32 | `200`, `text/html;charset=utf-8` | 138897 | `092DC6F0D73BE06A5C452848C20DCB8F9C614674F4E23263FDEE380A37072632` |
| <https://www.metacritic.com/browse/game/all/all/all-time/new/> | 1 | 11:20:34 | `200`, `text/html;charset=utf-8` | 66106 | `A3E4F6A65783AF8F0686B48BDC11E316F5EC2ED6A1D9A2E9DC67D8DE06E86A5D` |
| <https://www.metacritic.com/browse/game/all/all/all-time/new/> | 2 | 11:20:36 | `200`, `text/html;charset=utf-8` | 66099 | `34C517FE8F4633284215CA320BA6DD4688EE3A6C1AF174F319FC70D3DC92A9CD` |
| <https://www.metacritic.com/robots.txt> | 1 | 11:20:37 | `200`, `text/plain` | 364 | `DC0CB3EC87899D43B4A290E2848D98391DC4D5157EE6C29808D6492A3DB700EE` |
| <https://www.metacritic.com/robots.txt> | 2 | 11:20:37 | `200`, `text/plain` | 364 | `DC0CB3EC87899D43B4A290E2848D98391DC4D5157EE6C29808D6492A3DB700EE` |

Обе HTML-страницы вернули `Cache-Control: public, max-age=600` и обслуживались через Cloudflare. Разные хэши и `Last-Modified` у соседних HTML-ответов показывают, что содержимое изменчиво; повторяемым контрактом является успешный ответ и нужная структура, а не побайтовое равенство.

## Наблюдения

### Исходный HTML и динамическая загрузка

- Ответ `/game/` уже содержал строку `New Releases` и ссылки `/game/<slug>/`; найдено 86 уникальных ссылок на игры во всём документе.
- Ответ `/browse/game/all/all/all-time/new/` уже содержал `All New Games by Release Date` и ссылки `/game/<slug>/`; найдено 33 уникальных ссылки на игры во всём документе.
- Значит, на дату проверки начальный список не требует выполнения JavaScript только для обнаружения названий и URL игр.
- Эти page-wide counts не доказывают точное правило первых 20, пагинацию, полный контракт карточки, отзывы или платформенные оценки. Это предмет `SPK-02` после снятия блокера.

### Anti-bot и rate limit

- Две последовательные минимальные GET-пробы каждой требуемой страницы вернули `200`; `403`, `429`, login или challenge page не наблюдались.
- Нагрузочная проверка намеренно не выполнялась: опубликованного безопасного лимита не найдено, а после обнаружения ограничения Terms дополнительные автоматические запросы не оправданы.
- Результат не распространяется на будущую hosting-среду, другие IP/регионы, всплески частоты или последующие изменения Cloudflare.

### `robots.txt`

Для `User-agent: *` на момент проверки перечислены запреты `/search`, `/signup`, `/login`, `/user`, `/jl/`, `/8264/` и `/7336/`. Явных `Disallow` для `/game/` или `/browse/` нет. Отдельные именованные crawlers, включая `GPTBot` и `OAI-SearchBot`, запрещены полностью.

Это техническая crawl directive, а не лицензия на сбор, хранение или повторный показ данных. Повторная проверка нужна перед любым разрешённым live check.

### Применимые условия использования

Footer обеих страниц связывает Metacritic с Fandom и ведёт на официальный Fandom Terms of Use. На канонической странице [Terms of Use](https://www.fandom.com/terms-of-use) указана дата последней редакции 2025-12-19. В разделе о поведении пользователя условия, среди прочего:

- запрещают scraping, извлечение и индексирование контента роботами или retrieval-приложениями;
- требуют предварительного явно выраженного письменного разрешения для автоматизированного доступа;
- запрещают обход мер ограничения доступа и несоразмерную нагрузку;
- ограничивают копирование, агрегирование и эксплуатацию контента.

Прямая GET-проба URL Terms из footer вернула `403` дважды для исследовательского user-agent; текст был проверен через официальную каноническую страницу в browser-backed поисковом представлении. Ограничение доступа к документу само по себе не меняет опубликованное условие.

Почасовой сбор Metacritic с сохранением данных и отзывов в собственной базе попадает как минимум в область, для которой требуется явное разрешение. В репозитории такого разрешения, официального API, feed или другого одобренного интерфейса нет.

## Диспозиция рисков

| Риск | Решение | Основание | Остаточный риск |
|---|---|---|---|
| `R-EXT-01` | `Proceed with limitation` только для технической осуществимости | Локально обе страницы дважды вернули пригодный исходный HTML без browser session | Целевая среда не проверена; IP/region, Cloudflare, разметка и доступность могут измениться |
| `R-EXT-02` | `Ask` | Terms требует предварительного письменного разрешения для автоматизированного доступа, которого в проекте нет | Даже при разрешении нужно согласовать частоту, хранение, отзывы/media и публичный показ |

Итог `SPK-01` определяется более строгим ограничением `R-EXT-02`: `Ask`.

## Требуемое решение владельца

Нужно получить одно из следующих подтверждений:

1. письменное разрешение Fandom на автоматический почасовой доступ к указанным Metacritic pages и нужным game/review pages, а также на необходимое хранение и показ; или
2. одобренный API/feed/export с зафиксированными условиями; или
3. письменное изменение критериев задания, допускающее fixtures/предоставленный dataset вместо live Metacritic.

Если подтверждён вариант 1 или 2, перед `SPK-02` нужно повторить минимальную пробу из выбранной hosting-среды и установить conservative rate, timeout/backoff и cache policy. Если разрешённого пути нет, решение меняется на `Replan`; скрытая подмена обязательного источника недопустима.

## Достаточность и остаточные неизвестные

Spike остановлен после получения достаточного evidence для решения `Ask`:

- техническая доступность обеих заданных страниц локально повторена;
- наличие нужных данных в исходном HTML подтверждено на уровне списка;
- низкочастотное anti-bot поведение и изменчивость ответов зафиксированы;
- `robots.txt` и официальный Terms проверены раздельно;
- неизвестность целевой среды и непроверенный контракт данных не выданы за факт;
- дальнейший live scraping остановлен до разрешения.
