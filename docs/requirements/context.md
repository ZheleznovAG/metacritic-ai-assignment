# Контекст и рамки проекта

## Назначение

Документ фиксирует результат задачи `FOR-01`: какие рамки проекта известны из исходного задания, какие сведения отсутствуют и как отсутствие сведений учитывается на стадии формализации.

Он не выбирает архитектуру, стек, поставщиков или алгоритмы.

## Источники

| Приоритет | Источник | Состояние |
|---:|---|---|
| 1 | [`assignment.md`](../../assignment.md), SHA-256 `C8987F684CFDF693AB188FA2AC5875044C93EBF486B708C36FDF7E7748C2125C` | Зафиксирован |
| 2 | Письменные уточнения владельца | 2026-09-05 подтверждено разрешение для сформулированного Metacritic scope и выбрана бесплатная версия Grok; 2026-09-07 выбрана существующая Ubuntu VDS |
| 3 | Проверенные факты о внешних системах | Must-spikes сведены в [`G2 review`](g2_review.md); ограничения сохраняются |
| 4 | [`assumptions.md`](assumptions.md) | Допущения исполнителя, не исходные требования |
| 5 | Принятые проектные решения | [`ADR-0001`](../decisions/0001-minimal-stack-and-architecture.md) и [`design`](../design.md); не заменяют исходные требования |

## Известные рамки

- Нужно создать регулярно работающий сервис, а не одноразовый отчёт или скрипт.
- Обязательный источник игровых данных — Metacritic.
- Заявленная периодичность — один раз в час.
- Правило выборки имеет дневной цикл и партии по 20 игр.
- Требуется собственное сохраняемое представление данных.
- В продукте обязательны AI-резюме двух разных групп отзывов.
- Требуется пользовательский веб-интерфейс.
- Результат должен включать репозиторий, доступный сервис и AI-переписку.
- Есть две дополнительные части и произвольные улучшения; они не заменяют обязательную часть.
- Результат предписано отправить на адрес, указанный в исходном задании (`assignment.md`).
- Технический GET-доступ к двум заданным страницам Metacritic подтверждён локально 2026-09-04; 2026-09-05 владелец подтвердил наличие требуемого разрешения на сформулированный automated hourly access/storage/display scope.

## Отсутствующие сведения и их статус

| ID | Сведение | Что известно | Статус / дальнейшее действие |
|---|---|---|---|
| `CTX-01` | Дедлайн | В задании отсутствует | `Unknown`; отсутствие принято как ограничение относительного baseline, не текущий blocker. Запросить до календарного обещания; см. [план](../../implementation_plan.md#оценки-и-резерв) |
| `CTX-02` | Доступная ёмкость исполнителя | Не задана | `Unknown`; общий бюджет не вычисляется. Получить вместе с дедлайном до проверки календарной выполнимости |
| `CTX-03` | Денежный бюджет | Общий budget не задан; владелец разрешил только бесплатный AI API tier; `SPK-05` принял Groq Free Plan с `openai/gpt-oss-20b` и без paid fallback; существующая VDS проверена без новых платных обязательств | Не принимать новые платные обязательства; учитывать известный Free TPD bottleneck и очередь AI enrichment |
| `CTX-04` | Разрешённый/запрещённый стек | Внешних ограничений нет в тексте; обратимый project choice принят в `PLN-01` | Python 3.12 / Django 5.2 LTS / PostgreSQL 16 и Docker Compose production зафиксированы в [`ADR-0001`](../decisions/0001-minimal-stack-and-architecture.md); пересмотреть при конфликтующем уточнении |
| `CTX-05` | Hosting и инфраструктура | Ubuntu 24.04 VDS, 2 CPUs, 4,009,860 KiB RAM, persistent ext4, около 80 GB storage; owner-reported traffic — 32 TB. После настройки владельцем IMP-01 подтвердил Docker Engine 29.1.3, Compose 2.40.3 и daemon access пользователя deploy; HTTP preview доступен извне | [IMP-01 evidence](imp_01_review.md); отсутствие sudo не мешает Compose. Host reboot, persistence acceptance, DNS/TLS и application schedule checks остаются до G6; ранний preview их не закрывает |
| `CTX-06` | Аккаунты и локальные инструменты | Operator SSH/Groq settings находятся в ignored `.env` (`DEPLOY_SSH_*`); исследовательский `.venv` Python 3.14.7 сохранён. IMP-01 использует отдельный `.venv-app` Python 3.12.14, uv 0.12.11 и exact lock; локально Docker Engine 29.7.2 / Compose 5.4.0 | Application commands и tests — в `.venv-app`; `.env.app` содержит только application/DB settings. API/SSH secrets исключены из Git/image/web environment; запрет paid fallback сохраняется. См. [README](../../README.md) |
| `CTX-07` | Канал продуктовых уточнений | Указан только адрес отправки результата | `Unknown`; не использовать адрес для внешнего контакта без отдельного поручения |
| `CTX-08` | Срок доступности публичного сервиса | Не задан | `Unknown`; задокументировать выбранное обязательство перед сдачей |
| `CTX-09` | Требования к публичности репозитория | Нужна ссылка, модель доступа не задана | `Assumption`; см. `ASM-24` |
| `CTX-10` | Язык UI и AI-резюме | Владелец уточнил: исходные данные и отзывы не переводятся; AI-резюме сохраняет основной язык входного корпуса; язык статических UI-подписей пока не задан | Owner decision для source/summary language; UI-label assumption остаётся к выбору; см. `ASM-14` |
| `CTX-11` | Бизнес-часовой пояс | Не задан | `Assumption`; см. `ASM-01` |
| `CTX-12` | Поддерживаемые браузеры/устройства | Не заданы | `Assumption`; см. `ASM-25` |
| `CTX-13` | Нефункциональные SLO | Не заданы | Не изобретать production-SLO; проверять только необходимые для задания свойства |
| `CTX-14` | Точный состав AI-переписки | Формулировка допускает разные трактовки | `Assumption`; см. `ASM-23` |
| `CTX-15` | Правила автоматизированного доступа и хранения внешнего контента | Fandom Terms, связанный из footer Metacritic, требует предварительного письменного разрешения; 2026-09-05 владелец подтвердил наличие разрешения на сформулированный automated hourly access/storage/display scope | `Proceed with limitation`; сохранить подтверждение в AI-архиве, не расширять scope и повторно проверить Terms/`robots.txt` перед live deployment; см. `SPK-01` |

## Политика работы при отсутствии уточнений

1. Неизвестное не записывается как факт исходного задания.
2. Поведенческая неопределённость получает явное допущение в `assumptions.md`.
3. Внешний факт, который можно проверить, направляется в risk spike стадии 2.
4. Архитектурный выбор откладывается до результатов spikes.
5. Необратимые расходы, публикации и внешние обращения не выполняются без отдельного основания или разрешения.
6. Календарные оценки implementation-задач не фиксируются до появления `CTX-01–CTX-03`; относительные оценки workflow сохраняются.

## Проверка результата `FOR-01`

- [x] Все пробелы из intake получили ID.
- [x] Для каждого пробела указан статус и следующий механизм разрешения.
- [x] Отсутствующие сведения не подменены исходными требованиями.
- [x] Не выполнено внешних обращений или технических исследований.
