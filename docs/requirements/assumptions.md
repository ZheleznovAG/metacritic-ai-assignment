# Журнал допущений

## Назначение и статусы

Документ фиксирует результат `FOR-03`. Допущения нужны только там, где [`assignment.md`](../../assignment.md) допускает разные наблюдаемые варианты поведения.

Статусы:

- `Accepted provisionally` — принято исполнителем как простое обратимое правило до письменного уточнения;
- `Deferred to spike` — параметр или факт должен быть уточнён указанным исследованием до реализации;
- `Bonus only` — действует только если дополнительная часть выбрана после ворот `G6`.

Ни одно допущение ниже не объявляется требованием автора задания.

## Допущения обязательного контура

| ID | Вопрос | Принятое правило | Основание и влияние | Проверка / условие пересмотра | Статус |
|---|---|---|---|---|---|
| `ASM-01` | Какая зона задаёт «сегодня»? | Бизнес-зона конфигурируема; значение по умолчанию — UTC и явно показывается в документации/диагностике | UTC однозначен и не зависит от машины; смена зоны не требует менять бизнес-логику | Пересмотреть при уточнении автора; проверить границу суток в `SPK-04` | Accepted provisionally |
| `ASM-02` | Что значит «1 раз в час»? | Один плановый запуск инициируется в каждом часовом интервале по фиксированному расписанию; ручной запуск не заменяет плановый | Даёт проверяемое расписание и отделяет Bonus-кнопку | Уточнить допустимое отклонение после выбора hosting в `SPK-06` | Accepted provisionally |
| `ASM-03` | Что считается лимитом 20? | 20 уникальных кандидатов, выбранных для обработки в запуске; ошибка обработки не вызывает неограниченный добор в том же запуске | Делает нагрузку предсказуемой и не скрывает ошибки | Проверить модель переходов в `SPK-04`; пересмотреть при уточнении | Accepted provisionally |
| `ASM-04` | Когда игра «обработана сегодня»? | После успешной валидации и сохранения основных данных игры; AI-enrichment имеет отдельный статус и retry | Сбой AI не должен бесконечно занимать discovery-партию, но обязательное резюме всё равно отслеживается до успеха | Проверить восстанавливаемость в `SPK-04` и критериях `AI-03` | Accepted provisionally |
| `ASM-05` | Что происходит с failed item? | Неуспешная основная обработка остаётся retryable в тот же день; retry учитывается в лимите следующего запуска | Сохраняет лимит и обеспечивает повтор без отдельного бесконечного потока | Уточнить таблицей состояний в `SPK-04` | Accepted provisionally |
| `ASM-06` | Каков первый запуск дня? | Берутся до 20 подходящих позиций только из текущего New Releases в исходном порядке; SEE ALL в этот запуск не используется для добора | Следует последовательности пунктов 1 и 2 задания | Проверить фактический размер/порядок в `SPK-01–SPK-02` | Accepted provisionally |
| `ASM-07` | Как выбирать последующие партии? | Идти по SEE ALL в сортировке «Новые», пропускать обработанные сегодня и продвигаться по страницам до 20 кандидатов либо исчерпания источника | Делает «очередную страницу» воспроизводимым правилом и не возвращает дубли дня | Проверить пагинацию и порядок в `SPK-01–SPK-02` | Accepted provisionally |
| `ASM-08` | Что делать после исчерпания источника? | Завершать запуск успешно с нулём новых кандидатов и диагностическим счётчиком, не начинать выборку заново до нового дня | Предотвращает цикл и лишние запросы | Пересмотреть, если источник предоставляет иной конечный маркер | Accepted provisionally |
| `ASM-09` | Что сбрасывается в новый день? | Сбрасывается только дневной discovery-прогресс; накопленные игры и результаты не удаляются | Иначе требование обновлять существующую базу теряет смысл | Проверить в `SPK-04` и интеграционных критериях | Accepted provisionally |
| `ASM-10` | Что является одной игрой? | Одна игра объединяет платформенные варианты; первичный кандидат идентичности — канонический URL/slug Metacritic, не название | Соответствует фразе о многих платформах и снижает ложные совпадения по названию | Подтвердить или заменить по результату `SPK-03` | Deferred to spike |
| `ASM-11` | Как обновлять существующие данные? | Валидное непустое новое значение обновляет текущее; отсутствие/ошибка извлечения не затирает ранее корректное; платформы обновляются по идентичности платформы | Безопасно при частичной деградации источника | Проверить на fixtures и уточнить после `SPK-02–SPK-03` | Accepted provisionally |
| `ASM-12` | Какие поля допустимо не получить? | Название и подтверждённый источник/идентификатор обязательны для записи; естественно отсутствующие оценки, отзывы, изображение или видео представлены как «нет данных», а структурная потеря поля считается ошибкой | Отличает реальное отсутствие от поломки парсера | Классификацию полей подтвердить в `SPK-02` | Deferred to spike |
| `ASM-13` | Как трактовать несколько платформ? | Это одна карточка игры с коллекцией платформ; у каждой платформы своя пара Metascore/Userscore | Прямо следует из задания | Проверить на мультиплатформенной fixture в `SPK-02` | Accepted provisionally |
| `ASM-14` | На каком языке UI и резюме? | Основной UI и AI-резюме — на русском; исходные названия и собственные имена не переводятся | Язык задания — единственный доступный сигнал | Пересмотреть по письменному уточнению | Accepted provisionally |
| `ASM-15` | Как сортировать общий список по рейтингу? | Использовать Metascore: без фильтра — максимальный доступный по платформам; при фильтре — максимальный среди совпавших платформ; без оценки — после оценённых; равенство — по названию | Даёт однозначный результат для нескольких платформ и null | Зафиксировать тестами; пересмотреть, если UI предлагает явный выбор метрики | Accepted provisionally |
| `ASM-16` | Какие отзывы подавать в AI? | Критики и пользователи собираются раздельно; внутри группы используется воспроизводимая выборка всех успешно полученных отзывов до документированного лимита контекста | Не смешивает аудитории и ограничивает стоимость | Конкретный лимит и порядок определить в `SPK-05` | Deferred to spike |
| `ASM-17` | Когда данных для обобщения недостаточно? | Предварительный минимум — 3 содержательных отзыва одной аудитории; ниже показывается «Недостаточно данных» без обобщающего вывода | Снижает риск представить единичное мнение как консенсус | Проверить на eval-наборе и утвердить в `SPK-05` | Deferred to spike |
| `ASM-18` | Что значит «короткое резюме»? | Для каждой аудитории отдельные секции «Нравится» и «Не нравится», до 5 кратких тезисов в каждой | Непосредственно отражает формулировку задания и проверяется структурно | Утвердить по baseline `SPK-05` | Accepted provisionally |
| `ASM-19` | Когда обновлять резюме? | При изменении отпечатка входных отзывов либо версии prompt/model/подготовки входа; неизменный вход повторно не оплачивается | Реализует возможность обновления без бессмысленной регенерации каждый час | Подтвердить техническую осуществимость в `SPK-05` | Accepted provisionally |
| `ASM-20` | Сколько похожих игр показывать? | До 5 уникальных игр из собственной базы, без текущей игры; при малой базе допускается меньше или ноль | Компактно для карточки и корректно на малом наборе | Критерий релевантности и доступные признаки уточнить после `SPK-02` | Accepted provisionally |
| `ASM-21` | Что означает «похожая»? | Похожесть должна опираться только на сохранённые признаки игры, быть объяснимой и проходить заранее созданный golden set; конкретный алгоритм не фиксируется на стадии 1 | Не подменяет требование преждевременным техническим решением | Определить признаки/порог в `PLN-02` после внешних spikes | Deferred to spike |
| `ASM-22` | Что делать при сбое необязательного поля или enrichment? | Сохранить валидную основную запись, явно отметить неполноту и разрешить точечный повтор; не считать весь batch потерянным | Соразмерная изоляция ошибок для фонового сервиса | Проверить failure-сценариями после реализации | Accepted provisionally |

## Допущения поставки и интерфейса

| ID | Вопрос | Принятое правило | Основание и влияние | Проверка / условие пересмотра | Статус |
|---|---|---|---|---|---|
| `ASM-23` | Что входит во «всю переписку с нейросетью»? | Минимум — полная доступная история AI-assisted разработки в исходном порядке. Runtime-prompts, версии и eval-артефакты также публикуются; все сырьевые runtime-вызовы не считаются обязательными без уточнения | Формулировка находится в разделе результатов тестового и вероятнее относится к процессу разработки | Пересмотреть при уточнении; перед сдачей явно описать состав архива | Accepted provisionally |
| `ASM-24` | Какой доступ нужен проверяющему? | Репозиторий, сервис и AI-архив доступны проверяющему без запроса локальных секретов; read-only UI не требует авторизации | Иначе ссылки не выполняют демонстрационную функцию | Проверить из внешней сессии перед сдачей | Accepted provisionally |
| `ASM-25` | Какой клиент поддерживается? | Актуальный desktop Chromium — обязательная среда приёмки; базовая адаптивность не должна ломать чтение на узком экране | В задании нет матрицы браузеров; это минимальный воспроизводимый baseline | Пересмотреть при уточнении | Accepted provisionally |
| `ASM-26` | Можно ли редактировать AI-архив? | Секреты и персональные данные удаляются с явной записью о причине; содержательная история не переписывается и не реконструируется | Совмещает полноту и безопасность | Проверить manifest и secret scan перед сдачей | Accepted provisionally |

## Допущения только для Bonus

| ID | Вопрос | Принятое правило | Основание и влияние | Проверка / условие пересмотра | Статус |
|---|---|---|---|---|---|
| `ASM-B01` | Что значит «самый популярный летсплей»? | Максимальный публичный view count среди найденных видео, прошедших критерий «летсплей/gameplay»; trailers, shorts и обзоры исключаются | Даёт наблюдаемую метрику, но зависит от возможностей YouTube | Утвердить или отклонить в `BON-11` | Bonus only / Deferred to spike |
| `ASM-B02` | Что делать без доступного текста видео? | Показать отсутствие заключения и причину; не подменять ролик другим без повторной проверки популярности | Не допускает выдуманного transcript/заключения | Проверить в `BON-11–BON-12` | Bonus only |
| `ASM-B03` | Что означает monitoring «в реальном времени»? | Изменение фактического статуса отражается в UI не позднее 5 секунд при доступной связи | Даёт измеримый пользовательский критерий без требования конкретного транспорта | Пересмотреть при выборе Bonus 2 и среды | Bonus only / Accepted provisionally |
| `ASM-B04` | Кто может нажать принудительный запуск? | Только явно разрешённый оператор; повторный клик и конфликт с плановым запуском не создают вторую работу | Защищает ресурс и данные; read-only UI остаётся публичным | Модель доступа определить перед `BON-22` | Bonus only / Deferred to decision |

## Аудит журнала

- [x] Рассмотрены все неоднозначности, перечисленные в методологии.
- [x] Для каждого допущения указаны основание, влияние и условие пересмотра.
- [x] Внешние факты не объявлены проверенными до стадии 2.
- [x] Конкретные архитектура, стек и алгоритмы не выбраны.
- [x] Bonus-допущения не влияют на ворота обязательного контура.

