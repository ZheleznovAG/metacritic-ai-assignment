# План действий: master tracker

- Baseline: **1.0**, задача `PLN-03`, 2026-09-09 (Asia/Novosibirsk).
- Bonus scope: `bonus2`.
- [Повторный аудит `6551e42`](docs/requirements/implementation_audit_6551e42.md) выявил 19 findings и подтвердил исправления R02/R15. После [R03–R06](docs/requirements/imp_04_snapshot_correction.md) и [серии оставшихся 15 corrections](docs/requirements/implementation_corrections_batch.md) открытых findings этого аудита нет. [24 исходных probes и observations](research/reviews/6551e42/README.md) сохраняются как историческое evidence.
- Разрешённая пользователем пачка завершена: 261 application tests и offline checks пройдены локально и в Linux/Docker; browser/CSS/mobile checks и screenshots сохранены. [Verification excerpts и source hashes](docs/evidence/audit-batch-verification.txt). Hosted/public evidence и gates этой серией не закрываются; application DB не мигрировалась.

## Источники истины и правила

[assignment.md](assignment.md) задаёт scope; [methodology.md](methodology.md) — процесс; [implementation_plan.md](implementation_plan.md) — результаты, декомпозицию, оценки и Requirement/Risk mappings. [ADR](docs/decisions/0001-minimal-stack-and-architecture.md), [design](docs/design.md), [acceptance](docs/requirements/acceptance.md), [assumptions](docs/requirements/assumptions.md) и [risks](docs/risks.md) сохраняют свои роли.

Этот файл — единственное место текущих task/gate статусов, зависимостей и ссылок на фактическое evidence. Описания задач и численные оценки не копируются сюда. Исторические review фиксируют состояние на дату проверки, а не конкурирующие текущие статусы.

1. Один цикл: одна задача → author → adversarial review → verify → один focused commit → стоп. Новая задача начинается следующим циклом. При внешнем blocker текущий цикл можно приостановить и взять одну независимую Ready-задачу отдельным циклом; незавершённая задача сохраняет Blocked.
2. Все зависимости и предыдущие ворота должны быть выполнены. Проверенный code/документ без evidence не получает Verified; material finding даёт Changes requested.
3. При human input: Blocked / Ask, один конкретный вопрос и needed-by gate; до ответа доступны только независимые задачи. Уже полученные решения действуют.
4. Пригодность метода не принимается до замороженного oracle; live source/provider checks отделены от deterministic CI.
5. Таблица задаёт **конъюнкцию** зависимостей. Колонка «Ветка»: base — всегда, bonus1/bonus2 — только после выбора в BON-00. Суффикс `?` означает зависимость GB только при активной ветке; после отказа невыбранные task rows получают Dropped с основанием.
6. При Verified ожидаемое evidence заменяется ссылкой на существующий артефакт/check. Плановые ссылки/имена не являются доказательством.
7. Для hosted CI разрешён candidate-коммит незавершённой задачи с её фактическим статусом. После разрешённого push записываются run URL и проверенный SHA; evidence/status оформляются отдельным focused-коммитом. CI проверяет и этот коммит. Candidate-коммит не закрывает задачу. Локальная проверенная correction может быть закоммичена отдельно при сохранении внешнего blocker.
8. Поле Bonus scope выше — единственный текущий выбор: pending до принятия BON-00, затем none/bonus1/bonus2/both со ссылкой на решение в evidence BON-00. Невыбранные ветви получают Dropped; обязательные задачи исключать нельзя.

Статусы: Planned — описано; Ready — можно брать; In progress — выполняется; Changes requested — требуется исправление; Verified — критерий доказан; Blocked — внешний вход отсутствует; Dropped — исключён только необязательный scope с основанием.

## Приоритет текущего цикла

2026-09-18: По явному решению владельца репозиторий переведён в **public** (`DEL-01`), что заменяет прежнее решение private + invite. Перед публикацией проверены 112 доступных коммитов / 861 уникальный blob и логи всех 54 CI-запусков по 14 известным приватным значениям и credential-patterns. Единственный остаток — email ревьюера в старом whitespace-check log запуска `34573424815`; удалены только логи, запись о запуске сохранена. Анонимные запросы к репозиторию, актуальному успешному CI и API возвращают `200`, API подтверждает `private=false`: [execution evidence](docs/evidence/repository-public-2026-09-18.json). Владелец также подтвердил отправку по email архива переписки, ссылки на сервис и ссылки на репозиторий с информацией об инвайте; это подтверждение состава отправки не подменяет требуемые `REL-05` копию письма, timestamp и сверку фактического вложения.

2026-09-17: `REL-03` scope corrected at the owner's request: session inclusion follows Git and preserved artifacts, not only dates or `cwd`. [Selection](docs/evidence/ai-history-selection.json): 29 main conversations and 44 substantive review workers, eight empty/internal/duplicate records omitted. [Review](docs/requirements/rel_03_review.md), [manifest](docs/evidence/ai-history-audit.json), [independent verification](docs/evidence/ai-history-verification.json). This is a local correction; it does not assert delivery or a new hosted verification.

2026-09-17: `REL-04` закрыт следующим циклом после re-freeze `REL-01`, по продолжению того же запроса владельца ("продолжай"): [review](docs/requirements/rel_04_review.md). Repository/service external-accessibility проверены (анонимный `404` на repo — ожидаемо private; анонимный `200` на публичный сервис; `/ops/status/` без регрессий); AI-архив из `REL-03` пересобран **заново из живых session-логов**, а не переиспользован — новый SHA-256, 22 276 строк, и попутно добавлены два новых слоя redaction, которых не было в исходном REL-03 pass: локальный Windows-путь (появляется в почти каждом tool call) и production operator username (защищено собственное имя/email владельца перед этой заменой — protect/redact/restore, т.к. username — подстрока личного email владельца). Явно задокументировано ограничение: SSH-логин deploy-пользователя НЕ редактируется в архиве — это обычное слово, невозможно отличить от литературного текста без порчи прозы, в отличие от единственной чётко цитируемой фразы в tracked-репозитории. Metadata-консистентность подтверждена: все документы ссылаются на один и тот же candidate `6ade911`/`sha256:422d16be...`. Финальный dated link-check — 783 ссылки/97 файлов, 0 реальных поломок. `REL-05` разблокирован с явным обязательством пересобрать архив ещё раз непосредственно перед фактической отправкой.

2026-09-17: Владелец явно попросил переписать git-историю, чтобы приватные идентификаторы не оставались там даже в закрытых коммитах ("нужно переписать историю, чтобы это не осталось в истории") — это отменяет более раннее решение "оставить как есть" по тому же вопросу. Подтверждён полный объём (все 4 категории, включая email ревьюера) и то, что `REL-01` candidate SHA будет пересобран заново. Выполнено `git filter-repo --replace-text` (5 литеральных замен, не regex: те же значения, что и в предыдущей redaction-записи ниже, заменены на те же generic placeholders — теперь во всех исторических blob'ах, а не только в текущем tip) на свежем клоне. Перед rewrite отдельным обычным коммитом (`1abb4c2`) сначала исправлена сама privacy-redaction narrative в этом файле — она ранее повторно цитировала оригинальные значения как "было/стало" вместо плейсхолдеров, то есть сама была источником утечки; этот же урок применён и к формулировке текущей записи. После rewrite: старый tip `1abb4c2` → новый `940296c`; исчерпывающая проверка (`git grep <pattern> $(git rev-list --all) --`, не pickaxe `-S`, то есть полный обход каждого дерева каждого коммита, а не только точек изменения) подтвердила 0 совпадений всех 5 паттернов во всей истории. Побочных повреждений не найдено: авторство коммитов (личные имя/email владельца — не production-секрет и в scope редакции не входили) не тронуто (filter-repo `--replace-text` не трогает author/committer поля), GitHub org в URL репозитория и общая фраза `deploy-user` (не логин, обычное слово) не пострадали. Новый SHA-256 `assignment.md` записан в `intake.md` рядом с исходным intake-хешем с явным объяснением расхождения — единственное изменение файла это плейсхолдер email, весь остальной текст побайтово идентичен. Это делает предыдущий frozen `REL-01` candidate (`f716480...`) устаревшим (все commit SHA изменились) — `REL-01` требует повторной полной заморозки по `methodology.md` §12.1 на новом кандидате, следующим циклом. Ранее найденный `DEL-01` blocker (email в git-истории мешал сделать репозиторий public) теперь технически снят, но решение о видимости остаётся **private + collaborator invite** — смена на public не выполняется без отдельного явного поручения.

2026-09-17: Владелец отправил GitHub collaborator invite ревьюеру и попросил privacy-redaction проход поверх текущего tracked snapshot (по замечаниям независимой проверки владельцем): production operator username, deploy SSH username, прямой production IP и локальный Windows-путь заменены generic placeholders (`<operator>`, `<deploy-user>`, `<production-ip>`, `<project-root>`) в затронутых evidence/review-файлах. Email ревьюера тоже убран из `intake.md`/`context.md` (собственных производных документов проекта) — но на этом этапе **не** из `assignment.md`: его SHA-256 был зафиксирован в `intake.md` как доказательство неизменности исходного задания, а редактирование блоба сломало бы этот hash без последующего git-history rewrite. Домен `v978670.hosted-by-vdsina.com` и GitHub `ZheleznovAG/...` оставлены — это и есть сдаваемые ссылки. (Продолжение и пересмотр этого решения — см. запись про `REL-01`/history rewrite ниже.)

2026-09-17: Владелец принял решение по открытому пункту `DEL-01`/`G7` из `REL-01`: репозиторий остаётся **private**, доступ ревьюеру даётся прямым GitHub collaborator invite (владелец выполняет сам через web UI — GitHub REST API для invite по email без известного username не сработал, `404`; надёжный путь — `PUT .../collaborators/<username>`, но username неизвестен). Переписывание git-истории не требуется. [docs/evidence.md](docs/evidence.md)'s `DEL-01` строка обновлена соответственно.

2026-09-17: `REL-01` закрыт по явному запросу владельца (после независимой проверки владельцем присланного снапшота): candidate `f716480`, image `sha256:445f0de0...`. Все 10 пунктов `methodology.md` §12.1 выполнены на одном кандидате — [полный review](docs/requirements/rel_01_review.md). Этот же цикл выявил реальный `DEL-01` blocker: репозиторий был `private` (внешний ревьюер видел `404` на прямой запрос); владелец выбрал сделать `public`, но при этом обнаружился email ревьюера из исходного задания в git-истории с первых коммитов — репозиторий немедленно возвращён в `private` (окно публичности — несколько минут). Владельцу дан честный трейдофф (rewrite history — меняет практически все commit SHA, на которые ссылается весь action_plan.md и review-документы; либо private + collaborator invite без переписывания истории); решение отложено владельцем («оставить как есть, решите позже»). AI-архив `REL-03` тем же циклом дополнен третьим слоем redaction (email ревьюера) и пересобран (11 сессий, включая одну найденную дополнительную). `REL-04` разблокирован; `G7` — нет, до отдельного решения по repo visibility.

2026-09-17: `REL-03` закрыт по явному запросу владельца (продолжение после `REL-02`): [AI-archive review](docs/requirements/rel_03_review.md) — 10 реальных Claude Code session JSONL (611-строчный форк-дубликат исключён без потери контента, поскольку он целиком содержится в своей продолженной сессии), хронологический manifest с честно задокументированным пробелом (нет raw-лога до 2026-09-11, хотя репозиторий начат 2026-09-01 — ранний период есть только как markdown в `research/methodology/`). Redaction в два слоя (known-value denylist из реальных local/VDS credentials + generic `KEY=value` pattern по имени ключа, не bare-hex heuristic — легитимные commit SHA/image ID должны выжить) применена и проверена: 0 известных секретов в результате, легитимные идентификаторы не тронуты, 20 328 строк валидного JSON. Архив вне git (`.artifacts/rel03-ai-history/ai-history.zip`, gitignored) — `assignment.md` просит его как отдельное приложение к письму, не как часть репозитория. Владельцу рекомендовано выборочно проверить архив самому перед фактической отправкой `REL-05`.

2026-09-17: `REL-02` закрыт по явному запросу владельца ("rel02 в первую очередь"): новый [docs/evidence.md](docs/evidence.md) сводит все 31 requirement ID к уже существующему evidence (без повторной проверки); README.md обновлён (реальный public URL и явно бессрочная доступность на время ревью, актуальный раздел про `BON-22`, ссылка на индекс из intro и Deployment boundary). `DEL-01`–`DEL-04` по собственному критерию задачи остаются ожидаемыми до финального прогона `REL-01`/`REL-04`. Следующие по цепочке — `REL-03` (archive manifest/privacy report) параллельно с `REL-01` после него.

2026-09-17: `BON-22` реализован и локально проверен (owner явно попросил полную реализацию, не урезанный scope): auth/DB sessions без registration/admin UI, `processing.management.commands.create_operator` для provisioning; явная write-allowlist вместо blanket-grant (`scripts/grant_manual_run_access.py`, новый `db_grants` compose-шаг между `migrate` и `web`); durable `ManualRunRequest`/`TriggerAdmission` с idempotency-first admission и 1/5мин + 6/час rate limit; `processing/dispatcher.py` — отдельный поток, переиспользующий `run_tick`'s lease/resume/execute машинерию через новый `run_manual`, hourly timer не меняется; `/ops/run/` POST с queued/busy/rate-limited/invalid/forbidden ответами (JSON 403 для anonymous/no-permission/CSRF, авторизация проверяется на каждый запрос, не скрытием кнопки). [Review и evidence](docs/requirements/bon_22_review.md). 400 application tests (44 новых, включая реальные конкурентные PostgreSQL-гонки на idempotency/rate-limit/dispatcher-claim и реальный DB-role grant test) и браузерный E2E Chromium зелёные; полный `scripts/check.py` пройден на изолированном Docker Compose проекте. Adversarial self-review в процессе разработки нашёл и закрыл 8 реальных дефектов (полный список в review), включая три, обнаруженных только реальной живой проверкой: два реальным браузером (`SESSION_COOKIE_SECURE`/`CSRF_COOKIE_SECURE` жёстко `True` ломали сессию на HTTP; отсутствующие `request`/`auth` template context processors скрывали успешный login от UI) и один — только настоящим публичным URL после апгрейда (`provision_db.py` при любом отдельном `docker compose run --rm migrate` незаметно повторно снимал write-allowlist BON-22, `/ops/login/` реально отдавал `500`; исправлено `--no-deps` и восстановлением grants). Публичный upgrade на VDS выполнен: реальный `<operator>` оператор (пароль сгенерирован и передан только на сервере, никогда не увиден этой сессией), реальный manual run (`id=36`) claim'нут уже работающим production-scheduler за секунду без перезапуска, второй независимый anonymous browser context подтвердил видимость публично, следующее scheduled-окно (`id=37`, 03:00Z) прошло автоматически. `BON-22` и `GB` закрыты.

2026-09-16: `BON-21` закрыт. Candidate `0b41b9f` прошёл локальные/Linux checks (356 application tests); CI-фикс `f01e455` (только `.github/workflows/ci.yml` и review-запись, код приложения не менялся — `git diff --stat 0b41b9f..f01e455` вне `docs/**`/`*.md`/`.github/**` пуст) прошёл [hosted CI 35085312032](https://github.com/ZheleznovAG/metacritic-ai-assignment/actions/runs/35085312032). Владелец явно разрешил SSH-доступ этой сессии; выполнен реальный upgrade-деплой на VDS (backup, отдельная остановка scheduler/worker перед аддитивной миграцией `processing.0003`, сохранённые row counts/volumes, подтверждённая image identity), публичный `smoke.py` (8/8, с VDS и с внешней машины без SSH-туннеля) и публичный `/ops/status/` подтвердили живые heartbeat и автономный `run_id=20` (scheduled 11:00Z, succeeded 20/20) на новом образе без вмешательства оператора. [Review и evidence](docs/requirements/bon_21_review.md), [execution evidence](docs/evidence/bon-21-public-deploy.json). `BON-00` фиксирует [выбор scope](docs/decisions/0002-bonus2-scope.md); контракт — [Candidate](docs/bonus2_design.md). `BON-22` теперь `Ready`. В этом же цикле `git fetch`/`git push origin main` из инструментальной сессии впервые прошли напрямую (HTTPS `origin`, без ssh-agent) и запушили `fcecd2d`, подтверждено [hosted CI 35120889923](https://github.com/ZheleznovAG/metacritic-ai-assignment/actions/runs/35120889923) — более ранняя запись ниже про обязательный push владельцем отражает состояние на момент `PUB-01` и относится к тому моменту, не к текущим возможностям инструмента. Абзацы ниже сохраняют историю предыдущих циклов, текущая очередь определяется таблицами.

[Очередь исправлений](docs/requirements/implementation_audit_6551e42.md#очередь-исправлений) закрыта локальными и Docker regressions. [Повторная проверка IMP-02](docs/requirements/imp_02_revalidation.md) завершена: Elden Ring прошла source → VDS DB → публичную карточку; retained-score provenance исправлен, 263 tests прошли локально/в hosted CI, corrected source `92c846a` развёрнут и повторно проверен. [SIM-EVAL-01](docs/requirements/sim_eval_01_review.md) закрыт: владелец принял frozen oracle 1.0.0 2026-09-14 без изменений; IMP-06 разблокирован. [Повторная проверка IMP-03](docs/requirements/imp_03_revalidation.md) завершена: реальный scheduler нашёл и закрыл живой баг (userscore-виджет искался по всей странице вместо своего контейнера, что либо падало на "null", либо подставляло чужую оценку из карточки отзыва); фикс подтверждён настоящим автоматическим тиком на границе часа. [Повторная проверка IMP-04](docs/requirements/imp_04_revalidation.md) завершена: реальный worker нашёл и закрыл живой баг (score-only отзыв с `quote:null`), затем один реальный Groq call (в рамках free tier) подтвердил весь AI/collection/queue контур end-to-end. [Повторная проверка IMP-05](docs/requirements/imp_05_revalidation.md) завершена: исправлен overflow длинного названия, 35 fixture/browser checks и 50 public checks пройдены; CI и VDS upgrade source 5a038d4 подтверждены. [IMP-06](docs/requirements/imp_06_review.md) завершён: genre Jaccard 1.0.0 прошёл frozen comparison; genre storage/provenance, 284 tests и hosted CI для source `813c5d0` подтверждены. [SIM-VER-01](docs/requirements/sim_ver_01_review.md) завершён: source `408bd62` на VDS, 292 tests, 56 fixture и 202 public browser checks; Elden Ring/Wo Long связаны по реальным сохранённым жанрам. [IMP-07](docs/requirements/imp_07_review.md) завершён: детерминированный Chromium E2E (293 tests, hosted CI 34969484170) и реальный публичный путь — source `37b44fb` развёрнут на VDS с backup, live scheduler discovery (19 processed, +1 новая игра), live worker/collector (401 tick, 12409 реальных отзывов), ровно 2 реальных Groq call (critic+user для Brigandine Abyss, оба succeeded в рамках free tier), реальный Chromium против публичного VDS подтвердил обе AI-карточки и honest-empty similar-games. `G4` закрыт. Следующий цикл — любой из `HRD-01`–`HRD-05` (независимы, доступны параллельно). Локальная application DB не мигрировалась; до её запуска нужны [миграции](README.md), затем обычный успешный collection/build для verified current heads; исторические incomplete данные не реконструируются.

Второй независимый `/code-review high` проход (запущен владельцем поверх уже закоммиченного diff) нашёл 10 кандидатов; ни один не затрагивал production-логику (diff — новая test-инфраструктура плюс doc/template/smoke правки). Два реальных coverage-регресса исправлены: `test_health.py` потерял единственную быструю Django unit-test проверку, привязанную к реальному отрендеренному содержимому `/` (assertion на `"not implemented yet"` убран без замены) — восстановлено на проверку `game-list__controls`, тот же реальный маркер, что теперь использует и `scripts/smoke.py`. `_ok_response` в `test_summaries_worker.py` параметризован опциональными `likes`/`dislikes` (со старым поведением по умолчанию), чтобы `test_e2e.py`'s `provider()` больше не пересобирал тот же JSON-конверт вручную независимой копией. Playwright-браузер в `test_e2e.py` теперь закрывается в `finally`, а сам journey вынесен в отдельный `_run_browser_journey`. Остальные находки (unguarded `next()` в `ScenarioGateway` вместо never-raise контракта, единичный hardcoded review-page кейс вместо таблицы, дублирование slug-parsing однострочника, `strict=False` в `zip`, `ScenarioGateway.__init__` без негативных параметров, "магические" границы polling-циклов) — test-only fixture-код без текущего дефекта; каждый требует гипотетического будущего расширения сценария, которого сейчас нет, и оставлен как есть. 293 теста и полный `scripts/check.py` перепройдены зелёными после исправлений.

`HRD-01` закрыт вторым, независимым циклом ([полный review](docs/requirements/hrd_01_review.md)): parser-input extension половина (оставшаяся после HTTP-adapter половины предыдущего цикла) закрывает `IMP-02`'s known limitation — деградация extraction (сломанная markup) была неотличима от natural tbd для metascore/userscore. `_parse_platform` теперь требует, чтобы `criticScoreSummary` резолвился в dict (каждый живой fixture несёт это как dict, только `score` внутри может быть natural null); `parse_game_detail` теперь требует хотя бы один `global-score-wrapper` контейнер для lead userscore (каждый живой fixture несёт этот контейнер даже при natural tbd). Оба regression-теста мутируют реальный Elden Ring fixture (не hand-built payload) и подтверждены упавшими на pre-fix коде. Adversarial review нашёл и закрыл диагностическую неточность (единое сообщение об ошибке для "ключ отсутствует" и "неверная форма" затрудняло бы triage `candidate.last_error`); второй finding (одна аномальная платформа теперь проваливает весь ingest игры вместо только своего score) задокументирован как явный trade-off, а не исправлен — он консистентен с уже принятым поведением тех же id/name/slug/relatedGameId проверок в той же функции. 312 tests и полный `scripts/check.py` зелёные. Следующий цикл — любой из `HRD-03`/`HRD-04`/`HRD-05` (независимы, доступны параллельно).

`HRD-03` закрыт следующим циклом ([review](docs/requirements/hrd_03_review.md)): единственный реальный пробел против exit criterion — ни один существующий concurrency-тест не запускал два genuine потока на реальном PostgreSQL (все использовали `TestCase`'s единственное соединение/транзакцию). Три новых `TransactionTestCase` доказывают, что singleton `ProcessingLease` и оба row-leased job-queue (`ReviewCollectionJob`, `SummaryJob`) корректно сериализуют настоящих конкурентов; каждый тест подтверждён провалом при временно снятой блокировке. Adversarial review закрыл дублирование harness и mypy quirk. Следующий цикл — любой из `HRD-04`/`HRD-05`.

`HRD-04` закрыт следующим циклом ([review](docs/requirements/hrd_04_review.md)): единственные реальные пробелы против exit criterion — ни один тест не проверял, что реальная transport-ошибка (timeout/connection error), дошедшая до `summaries.worker.process_job`, корректно изолируется от остальной очереди; и ни один тест не проверял структурную изоляцию prompt injection (untrusted review text не может вырваться из своего JSON-string значения). Оба закрыты новыми тестами, каждый подтверждён провалом при временно ослабленном коде. Independent review не нашёл findings. Следующий цикл — `HRD-05`.

`HRD-05` закрыт следующим циклом ([review](docs/requirements/hrd_05_review.md)): в отличие от предыдущих HRD-циклов, никакой diagnostics/storage-report возможности не существовало вовсе — `observability` был назван компонентом в `docs/design.md`, но реализован только для AI-очереди (`summaries/observability.py`, IMP-04). Добавлены `processing/diagnostics.py` (run/game/job/backlog по ID) и `processing/storage_report.py` (sizes, WAL best-effort под least-privilege ролью, disk headroom, три overhead ratio, которые O(unique reviews) оценка пропустила бы), обе доступны через `scripts/diagnose.py`/`scripts/storage_report.py` под тем же read-only web-role, что и публичный preview. Закрыты два security-пробела с нулевым покрытием: redaction API-ключа в error path и escaping untrusted source text в шаблоне (структурно подтверждено — `|safe`/`mark_safe` отсутствуют во всём `app/presentation`); bounded logs и atomic-rollback safety на нехватку storage подтверждены цитированием существующего evidence, не дублированием. Adversarial review нашёл и закрыл три находки (unstable/failed review jobs терялись в backlog агрегате; несогласованный scope между rollup'ами; bare except без savepoint мог отравить окружающую транзакцию на ожидаемом permission-denied) — оба существенных фикса подтверждены реальным провалом до восстановления. Представительный storage forecast на реальных данных остаётся явным scope `PUB-02`. 339 tests и полный `scripts/check.py` зелёные. `HRD-06` (общий suite + G5 review) теперь `Ready`.

`HRD-06` закрыт следующим циклом ([review](docs/requirements/hrd_06_review.md)): полный `scripts/check.py` (393 теста и все offline verifiers — static/build, unit/fixture/integration/failure/concurrency, AI/similarity, E2E) выполнен одним прогоном на коммите `ea710ce`. Аудит `docs/risks.md` по каждому риску, называющему `HRD-*` задачей проверки, подтвердил реальный evidence из `HRD-01`–`HRD-05` без необходимости расширения scope; risks.md сам не переписывается (correction-статусы принадлежат этому файлу по его собственному правилу). Runtime/API live checks остаются отдельно. `G5` закрыт этим же report. `HRD-01`–`HRD-06` полностью Verified.

Следующая зависимая задача — `PUB-01` (развёртывание release candidate: versioned image, supervised scheduler/worker, DNS/TLS, реальный публичный endpoint). Эта задача требует живого SSH-доступа к VDS, который в текущей сессии недоступен (passphrase-защищённый deploy-ключ, нет TTY для интерактивного разблокирования) — тот же блокер, что мешает push в `origin`. `PUB-01`–`PUB-03`/`G6` остаются `Planned` до разрешения этого доступа; независимых Ready-задач вне этой цепочки на данный момент нет.

Владелец разблокировал VDS SSH (отдельный non-passphrase ключ) и запушил накопленные коммиты; push из этой сессии остаётся заблокирован тем же ssh-agent/named-pipe ограничением sandboxed-окружения инструмента (прямой `ssh` работает, `git push`/`git fetch`, порождающие дочерний процесс, — нет), поэтому push продолжает делать владелец напрямую. С разблокированным VDS-доступом начат `PUB-01`: единственный реальный пробел против аудита `A01` — отсутствие permanently supervised scheduler/worker Compose-сервисов — потребовал design-решения о секретах worker'а (Groq ключ не может жить в `.env.app` по уже принятому правилу); задан один конкретный вопрос владельцу, принят вариант отдельного `.env.worker`. Compose/secrets-половина реализована и проверена локально реальным стеком.

`PUB-01` закрыт этим же циклом реальным upgrade-deploy на VDS ([review](docs/requirements/pub_01_review.md)): source `1fc3fda` развёрнут поверх работавшего `37b44fb` по существующей backup-first процедуре (config+DB backup, checksummed transfer, image identity verification, 9/9 smoke, сохранённые/выросшие row counts, неизменные named volumes, реальный внешний HTTP-запрос без SSH-туннеля — 200). По ходу найден и закрыт один реальный tooling-баг: `docker compose ... --wait` на VDS'ном Compose 2.40.3 отказывается ждать контейнер с disabled healthcheck (локальный Compose v5.4.0 такой проблемы не имеет) — сами контейнеры стартовали нормально, сломался только wrapper-скрипт; `deploy/README.md` исправлен, чтобы следующий deploy не упёрся в то же самое. В первые же минуты supervised scheduler/worker автономно и без вмешательства оператора обработали реальный час (`run_id=4 outcome=succeeded selected=20 processed=20`) и корректно, без единого нового Groq call, разметили новые игры с недостаточным числом отзывов как `insufficient_data`.

`PUB-02` закрыт тем же продолжающимся циклом ([review](docs/requirements/pub_02_review.md)): два genuinely последовательных реальных hourly windows (`run_id=4` 19:00Z и `run_id=5` 20:00Z, оба succeeded), причём владелец выполнил реальный `sudo reboot` ПОСЕРЕДИНЕ между ними (non-interactive sudo недоступен этой сессии — тот же класс ограничения, что и push) — все пять supervised-контейнеров восстановились автоматически без единой ручной команды, все row counts сохранены и выросли, следующий час прошёл штатно. Изолированный restore реального pg_dump в disposable PostgreSQL на отдельной сети подтвердил точное совпадение pre-upgrade row counts и был полностью снесён после проверки, ни разу не коснувшись рабочей БД. `HRD-05`'s diagnose/storage_report впервые отработали на реальных production-данных.

`PUB-03` закрыт тем же циклом ([review](docs/requirements/pub_03_review.md)): реальный Chromium (Playwright, уже project dependency) против публичного URL без SSH-туннеля прошёл все 15 проверок — список, комбинированный search+filter, полная карточка, ровно 2 summary-секции с реальными claims, переход на независимо обнаруженную scheduler'ом похожую игру ("Elden Ring: Tarnished Edition") по shared genres. На тот момент `G6` не закрывался: его собственное условие явно требует HTTPS/external E2E, а deploy оставался HTTP-only preview без домена/DNS/TLS.

Владелец предоставил hoster-domain `v978670.hosted-by-vdsina.com` (уже резолвится на IP VDS) и явно попросил закрыть TLS в рамках задачи, где это должно было решаться, — `PUB-01`. Добавлен `deploy/Caddyfile.production` (реальный домен, `auto_https` по умолчанию), `compose.production.yaml` теперь публикует `80`/`443` и монтирует этот файл (`18081` остался только на loopback); VDS'ный `.env.app` дополнен доменом в `DJANGO_ALLOWED_HOSTS` и `DJANGO_HTTPS=true`. Развёрнуто на живом VDS: Caddy получил настоящий Let's Encrypt-сертификат (`certificate obtained successfully`), внешний `curl` без `-k` принимает сертификат как доверенный, HTTP (`80`) отдаёт `308` редирект на HTTPS, полный `smoke.py` (8/8) прошёл и с VDS, и с внешней машины. Найден и задокументирован реальный побочный эффект: `DJANGO_HTTPS=true` глобален, поэтому старый loopback-путь `18081` (`X-Forwarded-Proto: http`) теперь тоже редиректит не-health пути на настоящий домен — ожидаемо, не регрессия; `deploy/README.md` обновлён. `G6` теперь закрыт: все `PUB-01`–`PUB-03` условия, включая HTTPS/external E2E, подтверждены на реальном сервисе. После `G6` — решение `BON-00` о Bonus scope.

## Исторические циклы до аудита 2026-09-12

`IMP-01` закрыт независимым adversarial review в отдельной сессии ([imp_01_independent_review.md](docs/requirements/imp_01_independent_review.md)): все заявленные exit criteria (build/tests/security posture, hosted CI run [34574112620](https://github.com/ZheleznovAG/metacritic-ai-assignment/actions/runs/34574112620), VDS redeploy) воспроизведены заново в этой сессии с идентичным результатом; найдена и исправлена одна тривиальная документационная неточность (устаревшая строка в README.md).

`IMP-02` закрыт ([imp_02_review.md](docs/requirements/imp_02_review.md)): реальный multiplatform game (Elden Ring) прошёл end-to-end от живого источника через непрозрачный `(source, source_game_id)` upsert до публичной read-only карточки; sanitised HTML/SSR fixtures и независимые expected values построены до parser; non-destructive merge, естественный `null` (Xbox One/PlayStation 4 Metascore) и атомарность core/job intent (forced rollback test) проверены; пять последовательных живых запуска против реального сайта подтвердили idempotency без дублей. Три раунда adversarial review — собственный `/code-review high` и два прохода независимой проверки кода отдельной параллельной сессией (`metacritic-ai-assignment-dc`), не запрошенной этой сессией, включая повторную проверку уже применённого исправления — нашли и закрыли семь реальных дефектов (несинхронизированный `source_game_platform_id`, `javascript:`-URL без проверки схемы, отсутствие проверки canonical/og:url против misrouted fetch, выбор первой попавшейся `game-title` записи вместо совпадающей по slug, потерянная provenance для fanned-out platform fetches, blank-string не защищённый non-destructive merge, provenance-указатель на неудавшийся fetch при сохранённом старом значении — закрыт разделением `last_changed_fetch` на отдельные Metascore/Userscore поля) до коммита; один known limitation (деградация extraction неотличима от естественного отсутствия) задокументирован для `HRD-01`. `IMP-03` теперь `Ready`; независимые циклы `REV-EVAL-01`/`SIM-EVAL-01` остаются доступны параллельно.

`IMP-03` закрыт ([imp_03_review.md](docs/requirements/imp_03_review.md)): полная модель состояний `SPK-04` (`ProcessingLease`/`ProcessingRun`/`CoreAttempt`, реальный `DailyCycle`/`DailyCandidate` state machine) реализована; Selector детерминированно покрывает 11 из 12 paper scenarios `SPK-04` (`PS-11` — AI failure — структурно тривиален до `IMP-04`; `PS-12` — non-UTC timezone — не реализован, UTC default `ASM-01` эксплицитно достаточен). Реальный scheduler (`scripts/run_scheduler.py`, отдельная `SCHEDULER_DB_USER` роль) трижды подряд сработал точно на реальной часовой границе без вмешательства (12:00, 13:00 и 14:00 UTC), real retry двух подлинных transient-сбоев сети (не смоделированных) и real same-hour idempotency (skipped_duplicate без лишних HTTP). Живая проверка нашла и закрыла один реальный баг парсера, обнаруженный собственным вторым запуском scheduler'а: canonical-проверка SEE ALL страниц ошибочно требовала точное совпадение `?page=N`, хотя реальный источник canonicalизирует все страницы листинга к одному base URL независимо от номера страницы (подтверждено на трёх реальных снимках: page 1/2/7429); фикс — path-only проверка только для browse-листинга, строгая проверка для game-detail/platform страниц не тронута; процесс перезапущен с фиксом после второго запуска, и уже третий запуск (14:00 UTC) прошёл с фиксом автоматически — `browse_page` успешно получена, курсор продвинулся, статус `succeeded` — так фикс подтверждён не только прямым live-вызовом парсера, но и реальным automated tick. Финальный adversarial self-review (отдельный fork-проход по всему diff) нашёл второй реальный баг до коммита: `Selector.run_batch` ошибочно классифицировал batch, где отказали вообще все элементы, как `partial` вместо `failed` (расходится с таблицей статусов `SPK-04`); исправлено, добавлен регрессионный тест. `IMP-04` остаётся `Planned` в ожидании `REV-EVAL-01`; независимые циклы `REV-EVAL-01`/`SIM-EVAL-01` остаются доступны параллельно.

`REV-EVAL-01` закрыт ([rev_eval_01_review.md](docs/requirements/rev_eval_01_review.md)): 8 синтетических full-corpus cases (`evals/review_selection/cases.json` `1.0.0`) покрывают все требуемые категории — multi-page/platform, first-page bias, last-page bias, sentiment skew, редкая тема, cross-platform дубли, длинный текст с token-boundary truncation, семиязычный multilingual mix, изменение отзыва вне sample. Метрика — 8 бинарных hard invariants (не rubric: partial credit не имеет смысла для structural correctness), заземлённых в уже принятых `ASM-16`/`ASM-17`/`ASM-19` и pagination-контракте `SPK-02`, плюс явный non-goals раздел (sentiment/rare-topic representativeness — observational, не gated, поскольку принятая `ASM-16` policy не content-aware). `baseline_naive.py` (простой baseline из формулировки задачи) корректно проваливает ровно 3 из 8 invariants (`INV-PAGE-COVERAGE` дважды, `INV-DEDUP` один раз) — commited evidence в `baseline_report.json`, доказывающая discriminative power oracle до появления реального `IMP-04` candidate. 9/9 meta-tests scorer'а (`test_score_selection.py`) подтверждают, что сам scorer ловит каждый класс дефектного selector'а. Финальный adversarial self-review (отдельный fork-проход) нашёл два overclaim в evidence-документе (заявленный "десятиязычный" mix на деле семиязычный; детерминизм/content-independence naive baseline описаны как "легко получить случайно", хотя они гарантированы конструкцией sort key, не читающего content) и один реальный пробел (`INV-CONTENT-INDEPENDENCE` был structural no-op для любого case с пулом ≤10 — нечего исключать) — все три исправлены (invariant теперь `n/a` при пуле ≤10 review, docs скорректированы) до Ask. Автор не видел и не писал `IMP-04` код до заморозки. Согласно правилу 3 методологии задан один конкретный `Blocked / Ask`; владелец принял oracle как frozen 2026-09-12 без изменений. `IMP-04` разблокирован по этой зависимости.

`IMP-04` закрыт ([imp_04_review.md](docs/requirements/imp_04_review.md)): полная схема `docs/design.md` для сбора отзывов, построения corpus и AI-резюме реализована (`catalog.SourceFetch` расширен review-page evidence; `reviews` app — реальный `ReviewCollectionJob` state machine, `Review`/`ReviewObservation`/`ReviewCorpus`/`ReviewCorpusItem`; новый `summaries` app — `SummaryJob`/`SummaryAttempt`/`ReviewSummary`/`SummaryClaim`). Реальный backend review-page gateway/parser против `backend.metacritic.com` с свежими sanitised fixtures (Bayonetta xbox-360, 6 файлов, независимо построенный oracle). `reviews/selection.py` реализует точную ASM-16 candidate `1.0.0` policy и проверен против принятого `REV-EVAL-01` oracle отдельным `verify_candidate.py` (не меняющим frozen файлы) — **8/8 invariants пройдено**, там где naive baseline проходил только 5/8; теперь входит в автоматический `scripts/check.py`. `tiktoken` стал core-зависимостью проекта в этом цикле. Row-leased durable collector (`reviews/collector.py`) и production Groq adapter/worker (`summaries/{contour,groq_adapter,quota,worker,observability}.py`) с реальной HTTP-механикой, персистентным quota admission (без отдельной таблицы-счётчика — ledger это закоммиченные `SummaryAttempt`), cache-hit по fingerprint, explicit delayed_capacity без paid fallback. Реальный live-прогон (`scripts/run_worker.py`, отдельная `WORKER_DB_USER` роль) против 182 реальных `ReviewCollectionJob` из более ранних живых прогонов IMP-02/03: 30/30 реальных review_page fetches успешны, 776 реальных отзывов собрано, 5 маршрутов достигли terminal state (включая настоящий valid zero-total `empty`); для игры Orbitals (1 платформа) оба audience corpus/summary job создались автоматически по завершении маршрута, и **два реальных вызова Groq вернули настоящие grounded резюме** (openai/gpt-oss-20b, 2175 и 1942 токена) — карточка `/games/14/` отрисовывает оба честно, без сырых отзывов. Между двумя реальными Groq-вызовами живьём воспроизведена настоящая quota-задержка (`delayed_capacity`, `quota_exhausted`, ноль потраченной квоты) и последующее автоматическое восстановление после реального истечения окна — тот же механизм, что и в fake-provider test suite (`test_summaries_worker.py`, 8 тестов на cold/unchanged/changed/quota/malformed/insufficient/lease-loss сценарии), теперь подтверждён реальным clock. Живая проверка нашла один реальный диагностический дефект: `last_error` не очищался при последующем успехе (`succeeded` рядом со старым `quota_exhausted`) — исправлено в `collector.py` и `worker.py`. Финальный adversarial self-review (отдельный fork-проход по всему diff) нашёл два реальных дефекта до коммита: (1) ни `reviews/collector.py`, ни `summaries/worker.py` не восстанавливали job, оставшийся в `running` после падения воркера — `lease_expires_at` записывался, но нигде не читался, и `claim_next_job` никогда не рассматривал `running` строки, то есть падение воркера навсегда блокировало эту работу; исправлено добавлением `_recover_stale_leases` в оба claim-пути (по аналогии с `processing.selector`'s stale-candidate recovery, адаптировано под per-row lease вместо одного singleton lease), с немедленным bump fencing token при обнаружении просроченной аренды; (2) `reviews/corpus.py::build()` содержал незащищённый check-then-create race (`filter().first()` без lock, затем `create()` без `get_or_create`) — под design.md's `SELECT ... FOR UPDATE SKIP LOCKED` моделью, рассчитанной на параллельных воркеров, два воркера могли одновременно пройти проверку и один падал с `IntegrityError`; исправлено try/except с возвратом выигравшей строки. Оба фикса подтверждены новыми регрессионными тестами, включая реальный two-thread `TransactionTestCase` для race-условия; полный suite (146 Django тестов) зелёный. `IMP-05` теперь разблокирован; независимый `SIM-EVAL-01` остаётся доступен параллельно.

`IMP-05` закрыт ([imp_05_review.md](docs/requirements/imp_05_review.md)): `catalog.queries.list_games()`/`list_platform_options()` реализуют точное правило `ASM-15` (без фильтра — максимум по всем платформам игры; с фильтром — максимум только среди совпавших платформ; без оценки — после оценённых; равенство — по названию), case-insensitive substring поиск и platform filter. `presentation.views.index`/`game_detail` — stateless, весь state в query string (`q`/`platform`), без server-side session; ссылка "← Back to results" на карточке — весь механизм для AC-UI-06 без нового persisted state. 20 новых тестов (33 unit + integration), включая точный round-trip test URL списка. Живой прогон против 41 реальной игры из IMP-02/03/04: поиск `?q=elden` вернул ровно 2 реальные записи Elden Ring, фильтр `?platform=pc` сузил список с 41 до 34, комбинация `?q=elden&platform=pc` показала реальный PC Metascore (94) вместо кросс-платформенного максимума (96) — конкретное подтверждение ASM-15's filtered-vs-unfiltered различия на живых данных. Три реальных скриншота (`docs/evidence/imp05-*.png`, one-off local `playwright`, не добавлен в pyproject.toml/uv.lock): список с реальными обложками, карточка Elden Ring с честным `pending` для обоих резюме (не все маршруты ещё terminal), карточка Orbitals с двумя реальными завершёнными Groq-резюме из IMP-04. UI-label language зафиксирован как English (уже де-факто конвенция, теперь explicit ASM-14 decision). Public VDS smoke и полный browser matrix явно отложены до PUB-03/ASM-25 по multi-gate trace table в docs/design.md — не единоличная обязанность IMP-05. Один механический fix: существующий IMP-01 тест `test_index_states_actual_scope` переведён с `SimpleTestCase` на `TestCase`, поскольку `/` теперь делает реальный DB query. Полный suite (166 Django тестов) зелёный. Независимый `SIM-EVAL-01` остаётся доступен; `IMP-06`/`SIM-VER-01`/`IMP-07` — следующие по цепочке зависимостей.

## Подготовка: G0–G2

| ID | Зависимости | Ветка | Статус | Evidence |
|---|---|---|---|---|
| [IN-01](intake.md) | — | base | Verified | [Intake](intake.md) |
| [G0](intake.md) | IN-01 | base | Verified | [Intake gate](intake.md) |
| [FOR-01](docs/requirements/context.md) | G0 | base | Verified | [Контекст](docs/requirements/context.md) |
| [FOR-02](docs/requirements/requirements.md) | FOR-01 | base | Verified | [Реестр](docs/requirements/requirements.md) |
| [FOR-03](docs/requirements/assumptions.md) | FOR-02 | base | Verified | [Допущения](docs/requirements/assumptions.md) |
| [FOR-04](docs/requirements/acceptance.md) | FOR-02, FOR-03 | base | Verified | [Приёмка](docs/requirements/acceptance.md) |
| [FOR-05](docs/requirements/g1_review.md) | FOR-04 | base | Verified | [Trace review](docs/requirements/g1_review.md) |
| [G1](docs/requirements/g1_review.md) | FOR-05 | base | Verified | [G1 review](docs/requirements/g1_review.md) |
| [RSK-01](docs/risks.md) | G1 | base | Verified | [Риски](docs/risks.md) |
| [SPK-01](research/feasibility/metacritic-access.md) | RSK-01 | base | Verified | [Доступ](research/feasibility/metacritic-access.md) |
| [SPK-02](research/feasibility/metacritic-contract.md) | SPK-01 | base | Verified | [Контракт источника](research/feasibility/metacritic-contract.md) |
| [SPK-03](research/feasibility/game-identity.md) | SPK-02 | base | Verified | [Identity](research/feasibility/game-identity.md) |
| [SPK-04](research/feasibility/processing-state.md) | RSK-01, FOR-03 | base | Verified | [Календарная модель](research/feasibility/processing-state.md) |
| [SPK-05](research/feasibility/ai-summary.md) | SPK-02 | base | Verified | [AI baseline](research/feasibility/ai-summary.md) |
| [SPK-06](research/feasibility/deployment.md) | RSK-01, FOR-01 | base | Verified | [VDS probe](research/feasibility/deployment.md) |
| [RSK-02](docs/requirements/g2_review.md) | SPK-03, SPK-04, SPK-05, SPK-06 | base | Verified | [Risk review](docs/requirements/g2_review.md) |
| [G2](docs/requirements/g2_review.md) | RSK-02 | base | Verified | [G2 review](docs/requirements/g2_review.md) |

## Implementation baseline: G3

| ID | Зависимости | Ветка | Статус | Evidence |
|---|---|---|---|---|
| [PLN-01](docs/decisions/0001-minimal-stack-and-architecture.md) | G2 | base | Verified | [ADR-0001](docs/decisions/0001-minimal-stack-and-architecture.md) |
| [PLN-02](docs/requirements/pln_02_review.md) | PLN-01 | base | Verified | [Исправленные контракты](docs/requirements/pln_02_review.md) |
| [PLN-03](docs/requirements/g3_review.md) | PLN-01, PLN-02 | base | Verified | [Baseline review](docs/requirements/g3_review.md) |
| `G3` | PLN-03 | base | Verified | [G3 review и offline checks](docs/requirements/g3_review.md) |

## Сквозная реализация: G4

| ID | Зависимости | Ветка | Статус | Evidence |
|---|---|---|---|---|
| [IMP-01](implementation_plan.md#imp-01) | G3 | base | Verified | [Revalidation 2026-09-14](docs/requirements/imp_01_revalidation.md): [hosted CI 34804143596](https://github.com/ZheleznovAG/metacritic-ai-assignment/actions/runs/34804143596), 261 application tests; source `8c76615` развёрнут на VDS, image identity, migrations, runtime permissions, external HTTP/CSS и ресурсы проверены. [Execution excerpts](docs/evidence/imp-01-revalidation-2026-09-14.txt). |
| [IMP-02](implementation_plan.md#imp-02) | IMP-01 | base | Verified | [Revalidation и provenance correction](docs/requirements/imp_02_revalidation.md): [CI 34808052113](https://github.com/ZheleznovAG/metacritic-ai-assignment/actions/runs/34808052113), 263 application tests; corrected source `92c846a` на VDS, 3 live ingest без дублей, все core fields/provenance, public card и desktop/mobile checks подтверждены. [Source/DB/public snapshots](docs/evidence/imp-02-revalidation-2026-09-14.json). |
| [IMP-03](implementation_plan.md#imp-03) | IMP-02 | base | Verified | [Revalidation 2026-09-14](docs/requirements/imp_03_revalidation.md): реальный scheduler нашёл и закрыл живой баг (`invalid_userscore` из-за несужен­ного поиска виджета на всей странице); фикс подтверждён реальным автоматическим тиком на границе часа (10:00 UTC, 20/20 processed); 265 local tests. |
| [REV-EVAL-01](implementation_plan.md#rev-eval-01) | G3, SPK-05 | base | Verified | [Oracle accepted 2026-09-12](docs/requirements/rev_eval_01_review.md): 8 cases, 8 hard invariants, naive-baseline evidence, owner Ask answered |
| [IMP-04](implementation_plan.md#imp-04) | IMP-03, REV-EVAL-01 | base | Verified | [Revalidation 2026-09-14](docs/requirements/imp_04_revalidation.md): реальный worker нашёл и закрыл живой баг (score-only отзыв с `quote:null` ошибочно считался невалидным); один реальный Groq call (grounded, 6 claims, в рамках free tier) подтвердил R01/R07/R09/R10/R13/R14/R18 end-to-end; 266 local tests. |
| [IMP-05](implementation_plan.md#imp-05) | IMP-04 | base | Verified | [Revalidation 2026-09-14 UTC](docs/requirements/imp_05_revalidation.md): overflow длинного названия исправлен; 35 fixture/browser checks, 50 public checks на 38 играх; [CI 34837423748](https://github.com/ZheleznovAG/metacritic-ai-assignment/actions/runs/34837423748) прошёл. Source `5a038d4` развёрнут на VDS, CSS/image identity и сохранность данных подтверждены; screenshots и execution evidence сохранены. |
| [SIM-EVAL-01](implementation_plan.md#sim-eval-01) | G3 | base | Verified | [Oracle 1.0.0 accepted 2026-09-14](docs/requirements/sim_eval_01_review.md): 14 synthetic cases, nDCG@5 mean ≥0.90 / each ≥0.80, 8 hard invariants; 14 meta-tests PASS; owner Ask answered — accepted as-is, no changes. Hosted CI initially failed on a missing `.dockerignore` allowlist entry; fixed and passed on [34828861545](https://github.com/ZheleznovAG/metacritic-ai-assignment/actions/runs/34828861545). |
| [IMP-06](implementation_plan.md#imp-06) | SIM-EVAL-01, IMP-02 | base | Verified | [Review и comparison](docs/requirements/imp_06_review.md): genre Jaccard 1.0.0 прошёл frozen oracle (mean/floor 1.0, все invariants); weighted candidate отвергнут. Genre DTO/storage/provenance и миграция проверены; 284 application tests PASS локально и в [hosted CI 34867974090](https://github.com/ZheleznovAG/metacritic-ai-assignment/actions/runs/34867974090), tested SHA `813c5d0afb68ae60d09afad660332d8bf4163057`; build, runtime и HTTP/CSS smoke PASS. |
| [SIM-VER-01](implementation_plan.md#sim-ver-01) | IMP-05, IMP-06 | base | Verified | [Review](docs/requirements/sim_ver_01_review.md): 292 application tests, 56 fixture и 202 public browser checks; [CI 34929125538](https://github.com/ZheleznovAG/metacritic-ai-assignment/actions/runs/34929125538), tested SHA `408bd62bce72b2bfb6bfb53788b50bf9db618b28`. Точный image развёрнут с backup и `catalog.0006`; обычный ingest сохранил общие жанры Elden Ring/Wo Long, ID navigation и query continuity подтверждены на публичном preview. |
| [IMP-07](implementation_plan.md#imp-07) | IMP-03, SIM-VER-01 | base | Verified | [Review](docs/requirements/imp_07_review.md): детерминированный Chromium E2E (fixtures/fakes) и реальный публичный путь — live scheduler discovery (19 processed), live worker/collector (401 ticks, 12409 реальных отзывов), ровно 2 реальных Groq call (critic+user для нового Brigandine Abyss, оба succeeded), реальный Chromium против публичного VDS. |
| `G4` | IMP-07 | base | Verified | [IMP-07 report](docs/requirements/imp_07_review.md) + накопленное evidence IMP-01–IMP-07/SIM-EVAL-01/SIM-VER-01 покрывает все functional Must |

## Упрочнение: G5

| ID | Зависимости | Ветка | Статус | Evidence |
|---|---|---|---|---|
| [HRD-01](implementation_plan.md#hrd-01) | G4 | base | Verified | [Review](docs/requirements/hrd_01_review.md): HTTP-adapter половина закрыта — независимый аудит нашёл `A07` (нет bound на размер/время ответа, нет retry/backoff). Добавлены streaming с bounded size (8MiB)/time (30s), bounded retry только для transient failures. Adversarial review (два прохода) нашёл и закрыл три реальных дефекта: `ResponseNotRead` crash на каждом нормальном ответе, retryable=True для нетранзиентных httpx-ошибок, compression bomb через decoded-size check после decompression. Parser-input extension половина закрыта отдельным циклом: `IMP-02`'s known limitation (деградация extraction неотличима от natural tbd) исправлена для metascore (`criticScoreSummary` должен резолвиться в dict) и lead userscore (`global-score-wrapper` контейнер должен существовать); два regression теста мутируют реальный Elden Ring fixture и подтверждены упавшими на pre-fix коде. Adversarial review нашёл и закрыл одну диагностическую неточность (сообщение об ошибке не различало "ключ отсутствует" от "неверная форма"); второй finding (одна аномальная платформа теперь проваливает всю игру) задокументирован как принятый trade-off, консистентный с уже существующими identity-проверками той же функции. 312 tests зелёные, полный `scripts/check.py` PASS. |
| [HRD-02](implementation_plan.md#hrd-02) | G4 | base | Verified | [Review](docs/requirements/hrd_02_review.md): независимый аудит нашёл `A05` — scheduler навсегда терял восстановление в текущем часовом слоте после сбоя до/после lease; исправлено безопасным resume того же `ProcessingRun` под `select_for_update`+existing lease machinery; adversarial review нашёл и закрыл race в самом фиксе (stale snapshot при конкурентном resume); 298 tests зелёные. Попутно закрыт `A06` (Groq client timeout игнорировался). |
| [HRD-03](implementation_plan.md#hrd-03) | G4 | base | Verified | [Review](docs/requirements/hrd_03_review.md): аудит существующего suite нашёл, что stale owner/reclaim/late response/redelivery уже покрыты реальным PostgreSQL 16 в предыдущих циклах, но исключительно через `TestCase` (одна транзакция/соединение) — ни один тест не доказывал, что `select_for_update()`/`select_for_update(skip_locked=True)` реально сериализует genuine concurrent claimants. Добавлены три `TransactionTestCase` с настоящими потоками и `threading.Barrier` для `processing.lease.acquire_lease`, `reviews.collector.claim_next_job` и `summaries.worker.claim_next_job`; каждый подтверждён five-of-five/three-of-five провалом при временном снятии лока перед восстановлением фикса. Adversarial review нашёл и закрыл дублирование test harness (вынесено в `tests/concurrency.run_concurrently()`) и попутно реальный mypy inference quirk. 317 tests и полный `scripts/check.py` зелёные. |
| [HRD-04](implementation_plan.md#hrd-04) | G4 | base | Verified | [Review](docs/requirements/hrd_04_review.md): аудит существующего suite нашёл 4 из 6 пунктов exit criterion уже покрытыми (malformed output, quota exhaustion, сохранение предыдущего summary, capacity/cache/version, frozen quality eval уже нацелен на текущий shipped contour — версии `prompt`/`schema` в baseline run совпадают с `contour.py`). Два реальных пробела закрыты: timeout isolation (`TransportFailureTests`, реальный `httpx.ReadTimeout`/`ConnectTimeout` через worker) и структурная изоляция prompt injection (`PromptInjectionIsolationTests` — untrusted review text не может вырваться из своего JSON-string значения и изменить роли сообщений). Оба теста подтверждены провалом при временно ослабленном коде перед восстановлением фикса. Independent `/code-review high` (включая реальный PostgreSQL run) не нашёл findings. 321 tests и полный `scripts/check.py` зелёные. |
| [HRD-05](implementation_plan.md#hrd-05) | G4 | base | Verified | [Review](docs/requirements/hrd_05_review.md): `observability` компонент дополнен `processing/diagnostics.py` (run/game/job/backlog по ID) и `processing/storage_report.py` (DB/table sizes, WAL best-effort, disk headroom, versions/observations/attempts overhead — не O(unique reviews)); оба доступны через `scripts/diagnose.py`/`scripts/storage_report.py` под read-only web-ролью. Закрыты два security-пробела: redaction API-ключа в error path (`ApiKeyRedactionTests`) и escaping untrusted source text в шаблоне (`test_untrusted_source_text_is_escaped_not_rendered_as_html`); bounded logs и atomic-rollback safety подтверждены существующим evidence. Adversarial review нашёл и закрыл три находки: unstable/failed review jobs исключались из backlog агрегата, несогласованный scope между двумя rollup'ами в одной функции, и bare except без savepoint мог "отравить" окружающую транзакцию при ожидаемом permission-denied на `pg_ls_waldir()` — оба подтверждены реальным провалом (403-путь и реальный aborted-transaction) до восстановления фикса. Представительный storage forecast на реальных данных — явно отложенное scope `PUB-02`, не дублируется здесь. 339 tests и полный `scripts/check.py` зелёные. |
| [HRD-06](implementation_plan.md#hrd-06) | HRD-01, HRD-02, HRD-03, HRD-04, HRD-05 | base | Verified | [Review](docs/requirements/hrd_06_review.md): полный `scripts/check.py` (393 теста и все offline verifiers: static/build, unit/fixture/integration/failure/concurrency, AI/similarity, E2E) прошёл одним прогоном на коммите `ea710ce`. Аудит `docs/risks.md` по каждому риску, называющему `HRD-*` задачей проверки (`R-EXT-01/03/04`, `R-AI-01/02`, `R-ID-01`, `R-TIM-01/02`, `R-DAT-01`, `R-OPS-01`, `R-TST-01`, `R-SEC-01`), подтвердил реальный evidence из уже закрытых `HRD-01`–`HRD-05`; ни один не потребовал расширения scope. Runtime/API live checks остаются отдельно (никаких live-вызовов в этом suite). |
| `G5` | HRD-06 | base | Verified | [HRD-06 report](docs/requirements/hrd_06_review.md) + накопленное evidence `HRD-01`–`HRD-06` покрывает отказные, календарные, recovery/concurrency, source validation, AI/similarity quality, diagnostics/security и полный suite |

## Публичная валидация: G6

| ID | Зависимости | Ветка | Статус | Evidence |
|---|---|---|---|---|
| [PUB-01](implementation_plan.md#pub-01) | G5 | base | Verified | [Review](docs/requirements/pub_01_review.md): permanently supervised `scheduler`/`worker` Compose-сервисы (`restart: unless-stopped`, собственные ограниченные роли, `x-background-service` anchor); новый `.env.worker` для Groq-секрета (owner выбрал этот вариант вместо `.env.app`/ручного режима). Adversarial review закрыл 3 находки в deploy/README.md и DRY-дублирование. Реальный upgrade-deploy на VDS (source `1fc3fda`, backup DB+config, checksummed image transfer) выполнен: image identity и 9/9 smoke checks прошли, все существующие table row counts сохранены и выросли, оба named-volume набора не изменились, реальный внешний HTTP-запрос (не через SSH-туннель) вернул 200. Найден и задокументирован один реальный tooling-баг (`docker compose ... --wait` несовместим с disabled healthcheck на Compose 2.40 на VDS — контейнеры стартовали нормально, сломался только wrapper-скрипт; deploy/README.md исправлен). В течение первых минут supervised scheduler/worker уже реально и автономно обработали текущий час (`run_id=4 outcome=succeeded selected=20 processed=20`), без единого нового Groq call (новые игры честно получили `insufficient_data`). TLS/DNS закрыты отдельным follow-up внутри этой же задачи после того, как владелец предоставил hoster-domain `v978670.hosted-by-vdsina.com`: `deploy/Caddyfile.production` (реальный домен, `auto_https`), `compose.production.yaml` публикует `80`/`443`, `.env.app` дополнен доменом/`DJANGO_HTTPS=true`. Живой Let's Encrypt-сертификат получен (`certificate obtained successfully`), HTTPS/HTTP-редирект/smoke (8/8) подтверждены и с VDS, и извне. Reboot/два последовательных окна — `PUB-02`/`PUB-03`. |
| [PUB-02](implementation_plan.md#pub-02) | PUB-01 | base | Verified | [Review](docs/requirements/pub_02_review.md): два genuinely последовательных реальных hourly windows (`run_id=4` 19:00Z, `run_id=5` 20:00Z, оба succeeded 20/20/0, с честным `skipped_duplicate` между ними) — с реальным контролируемым host reboot ПОСЕРЕДИНЕ (владелец выполнил `sudo reboot`, т.к. non-interactive sudo недоступен этой сессии), все 5 supervised-контейнеров автоматически восстановились без ручного вмешательства, все table row counts сохранены и выросли. Изолированный restore реального backup в disposable Postgres на отдельной сети подтвердил точное совпадение pre-upgrade row counts, затем полностью снесён. `HRD-05`'s diagnose/storage_report впервые запущены на реальных production-данных: 0 outstanding summary jobs, overhead ratios разумны, только 2 из 36 terminal jobs когда-либо стоили реального Groq call. Найдено и задокументировано ограничение: `--disk-path` внутри `web`-контейнера видит только его собственный маленький tmpfs, не host disk — реальный headroom сверен через `df -h` на хосте (71 GiB свободно). |
| [PUB-03](implementation_plan.md#pub-03) | PUB-02 | base | Verified | [Review](docs/requirements/pub_03_review.md): реальный Chromium (Playwright) против публичного URL без SSH-туннеля — список, комбинированный search+filter (Elden Ring найден по `?q=elden&platform=pc`), полная карточка, ровно 2 summary-секции ("Critics"/"Users") с реальными claims (не pending/insufficient), переход на "Elden Ring: Tarnished Edition" — независимо обнаруженную scheduler'ом похожую игру по shared genres, не seeded пару. Все 15 отдельных проверок прошли. |
| `G6` | PUB-03 | base | Verified | Все `PUB-01`–`PUB-03` условия подтверждены на реальном сервисе, включая HTTPS/external E2E: живой Let's Encrypt-сертификат для `v978670.hosted-by-vdsina.com` (`certificate obtained successfully`), доверенный `curl` без `-k`, HTTP→HTTPS редирект (`308`), полный `smoke.py` (8/8) и 15-проверочный Chromium-прогон [PUB-03](docs/requirements/pub_03_review.md) — оба с VDS и с внешней машины. Два последовательных hourly windows и host reboot — [PUB-02](docs/requirements/pub_02_review.md). |

## Условный Bonus: GB

| ID | Зависимости | Ветка | Статус | Evidence |
|---|---|---|---|---|
| [BON-00](implementation_plan.md#bon-00) | G6 | base | Verified | [ADR-0002](docs/decisions/0002-bonus2-scope.md): владелец уточнил scope «вторая часть»; [review](docs/requirements/bon_00_review.md), сверка кода и offline plan audit. Принят выбор ветки, технический контракт остаётся Candidate. |
| [BON-11](implementation_plan.md#bon-11) | BON-00 | bonus1 | Dropped | YouTube не входит в выбранную владельцем ветку: [ADR-0002](docs/decisions/0002-bonus2-scope.md). |
| [BON-12](implementation_plan.md#bon-12) | BON-11 | bonus1 | Dropped | YouTube не входит в выбранную владельцем ветку: [ADR-0002](docs/decisions/0002-bonus2-scope.md). |
| [BON-21](implementation_plan.md#bon-21) | BON-00 | bonus2 | Verified | [Review](docs/requirements/bon_21_review.md): candidate `0b41b9f` (356 application tests PASS локально/в Linux Docker), CI-фикс `f01e455` — [hosted CI 35085312032](https://github.com/ZheleznovAG/metacritic-ai-assignment/actions/runs/35085312032) success. Реальный upgrade-деплой на VDS: backup, scheduler/worker остановлены отдельно от web/db/caddy, аддитивная миграция `processing.0003`, все row counts и volumes сохранены, image identity подтверждена. Публичный `smoke.py` 8/8 с VDS и с внешней машины без SSH-туннеля; публичный `/ops/status/` показал живые heartbeat и автономный `run_id=20` (11:00Z, succeeded 20/20) на новом образе без вмешательства оператора. [Execution evidence](docs/evidence/bon-21-public-deploy.json). |
| [BON-22](implementation_plan.md#bon-22) | BON-21 | bonus2 | Verified | [Review](docs/requirements/bon_22_review.md): auth/DB sessions, явная write-allowlist (`scripts/grant_manual_run_access.py`, после `migrate`), durable admission/dispatcher (переиспользует `run_tick`'s lease/resume), Run processing UI, CSRF/permission boundary. 400 application tests (44 новых), реальные конкурентные PostgreSQL-гонки (idempotency/rate-limit/dispatcher), реальный DB-role grant test, браузерный E2E (Chromium); [hosted CI 35131996286](https://github.com/ZheleznovAG/metacritic-ai-assignment/actions/runs/35131996286) на `959c2d6`. Adversarial self-review нашёл и закрыл 8 реальных дефектов, включая production-баг, пойманный только настоящим публичным URL (`db_setup` re-run снимал write-allowlist, `/ops/login/` реально отдавал 500 — исправлено). Публичный upgrade на VDS: реальный оператор (пароль никогда не увиден этой сессией), реальный manual run (`id=36`, claim'нут живым scheduler'ом за секунду), второй независимый anonymous browser context, следующее scheduled-окно (`id=37`) без вмешательства. [Execution evidence](docs/evidence/bon-22-public-deploy.json). |
| `GB` | BON-00, BON-12?, BON-22? | base | Verified | [ADR-0002](docs/decisions/0002-bonus2-scope.md) зафиксировал `bonus2`; `BON-11`/`BON-12` (bonus1) — Dropped с основанием; `BON-21`/`BON-22` — Verified с реальным public/failure/concurrency evidence, без Must-regression. |

## Комплект и сдача: G7

| ID | Зависимости | Ветка | Статус | Evidence |
|---|---|---|---|---|
| [REL-02](implementation_plan.md#rel-02) | GB | base | Verified | [docs/evidence.md](docs/evidence.md): полный requirement→evidence индекс (RUN/SEL/DATA/AI/UI/SIM/OPS/DEL/NFR, `YT-01` отмечен Dropped); README.md обновлён (реальный public URL, срок доступности — бессрочно на время ревью, актуальный раздел Manual run BON-22, ссылка на индекс). `DEL-01–04` отмечены ожидаемыми до финального прогона `REL-01`/`REL-04`, не Verified — по собственному критерию задачи. |
| [REL-03](implementation_plan.md#rel-03) | GB | base | Verified | [Review](docs/requirements/rel_03_review.md): 29 main conversations + 44 substantive review workers, each linked to Git/artifacts; 15 adversarial tests, Ruff, ZIP/JSON/member-hash checks and [independent privacy scan](docs/evidence/ai-history-verification.json) passed. Truncated-key redaction corrected after the independent scan rejected an intermediate export. [Manifest](docs/evidence/ai-history-audit.json) owns the current counts/hash; `REL-05` still refreshes the final cutoff. |
| [REL-01](implementation_plan.md#rel-01) | REL-02, REL-03 | base | Verified | [Review](docs/requirements/rel_01_review.md): re-frozen after the git-history rewrite, candidate `6ade911`, image `sha256:422d16be...` (supersedes the same-day `f716480`/`sha256:445f0de0...`, whose commit no longer exists post-rewrite). Все 10 пунктов `methodology.md` §12.1 пройдены заново на новом кандидате: реальный чистый `git clone` в изолированный `COMPOSE_PROJECT_NAME`, полный `scripts/check.py` (ruff/mypy/400 tests + 7 db-grant tests/evals/E2E), публичный smoke 8/8, четыре реальных подряд идущих scheduled-окна (run 40/41/42/43), restart-evidence переиспользован (deployed image побайтово не изменился), secret scan (полный tree-walk по всей переписанной истории, не pickaxe — 0 совпадений), 781 relative-ссылка во всех 97 tracked `.md` файлах резолвится, 29 hosted CI run URL доступны для authenticated viewer (уточнение: `200` только с авторизацией — анонимный запрос отдаёт `404`, т.к. репозиторий private; проверено напрямую). `REL-04` разблокирован. |
| [REL-04](implementation_plan.md#rel-04) | REL-01 | base | Verified | [Review](docs/requirements/rel_04_review.md): repository anonymous 404 (private, expected) / authenticated reachable; публичный сервис анонимно `200`, `/ops/status/` показывает run 43 (09:00Z) без регрессий; вся документация цитирует один и тот же candidate `6ade911`/`sha256:422d16be...`; финальный dated link-check — 783 ссылки/97 файлов чисто. AI-архив переоткрыт после материальной находки (Codex CLI сессии, см. `REL-03` выше) и пересобран второй раз тем же циклом — актуальный SHA-256 и полный manifest в [REL-03](docs/requirements/rel_03_review.md), не дублируется здесь. `REL-05` разблокирован с явным обязательством пересобрать архив ещё раз непосредственно перед отправкой. |
| [REL-05](implementation_plan.md#rel-05) | REL-04 | base | Planned | Ожидается: Отправленное сообщение и timestamp |
| `G7` | REL-05 | base | Planned | Ожидается: Release checklist + DEL-04 receipt |

## Условия ворот

Статус ворот находится только в таблицах выше. Выполнение зависимостей необходимо, но не заменяет содержательный gate review:

| Gate | Условие принятия |
|---|---|
| G0 | Исходник/обязательный и Bonus scope/комплект сдачи сохранены без подмены неизвестного |
| G1 | Все требования имеют ID, трактовку, критерий и ожидаемый evidence; неоднозначности видимы |
| G2 | Must-spikes имеют проверенный результат, решение и остаточный риск; Bonus изолирован |
| G3 | Полное Requirement/Risk покрытие; обоснованный стек; ацикличные зависимости; ранний public slice; fixtures/evals/failures/delivery и резерв учтены; статусы/описания не дублируются |
| G4 | Продуктовые функциональные Must RUN/SEL/DATA/AI/UI/SIM имеют первичное evidence, рабочий source→DB→UI путь, безопасное обновление, оба резюме/похожие игры, полный deterministic E2E и публичный срез |
| G5 | Отказные, календарные, recovery/concurrency, source validation и AI/similarity quality проверки проходят; diagnostics/security и полный suite подтверждены |
| G6 | Продуктовые и эксплуатационные Must подтверждены публично: реальные данные, HTTPS/external E2E, два последовательных application schedule windows, restart/redeploy/host-reboot persistence и isolated restore; capacity/storage ограничения измерены и не скрывают блокирующий Must-дефект |
| GB | BON-00 зафиксировал scope; выбранные ветки приняты с failure/eval/public evidence без Must-regression либо исключены; none закрывает gate записью решения |
| G7 | Все 28 Must, включая DEL-01–DEL-04, имеют evidence; candidate соответствует публичной версии; README/архив/ссылки проверены, секретов нет, ограничения открыты и отправка подтверждена |

`G6` не требует заранее выполнить отправку, архив и финальный repository/комплект check из `DEL-*`: эти delivery AC окончательно закрываются в `G7`. Это исправление зависимости ворот, не исключение требований. `G3` принимает план; оно не подтверждает production capacity, application tests или качество ещё не выбранных policies.

## Изменение baseline и сохранённая история

Baseline 1.0 (`PLN-03`) заменяет громоздкие task cards на master tracker и связанный implementation backlog, сохраняя все 46 Task IDs. Проверочные artifacts и git-история не удаляются. Предыдущий полный план и журнал 0.3–0.18 доступны через `git show 0c353ec:action_plan.md`; исходное задание имеет неизменный SHA-256 из intake.

Материальные изменения: IMP-04 зависит от готового календарного/ownership контура IMP-03; HRD-задачи явно стоят после G4; условные Bonus-ветки не блокируют none; REL-02/REL-03 готовят комплект до REL-01; G6/G7 разделяют public readiness и завершённую сдачу. Обоснование, проверка графа и явные ограничения бюджета — [g3_review.md](docs/requirements/g3_review.md).

Новый факт обновляет источник соответствующего решения/контракта, связанные задачи/риски и необходимые проверки. Нельзя задним числом ослаблять AC под реализацию или считать лимит timebox основанием для Verified.
